<%= dm_menu do %>  
  <%= dm_header %>

  <%= dm_nav do %>  
    <% if @current_organization %>  
      <li><%= link_to big_icon('home') + " Home", root_path %></li>

      <%= dropdown_menu big_icon('database') + ' ' + t(:models) do %>  
        <li><%= link_to fwicon('print') + " " + t(:printer_models), printer_models_path %></li>
        <li><%= link_to fwicon('cubes') + " " + t(:toner_models), toner_models_path %></li> 
      <% end %>

      <%= dropdown_menu big_icon('institution') + @current_organization.name do  %>
        <% @available_organizations.each do |organization| %>  
          <li><%= link_to icon('home') + ' ' + organization.short_description, root_path %></li>
        <% end %>
      <% end %>

      <!-- ricerca -->
      <%= form_tag search_path, class: "navbar-form navbar-right", role: "search" do %>
        <div class="form-group">
          <input name="search_string" size="6" type="text" class="form-control" placeholder="cerca" />
        </div>
      <% end %>

      <li><%= link_to big_icon('question'), helps_path %></li>
    <% end %>
  <% end %>
<% end %>

