<%= horizontal_form_for @vendor do |vendor| %>
  <%= vendor.dm_error_notification %>  

  <%= vendor.input :name, hint: "Per esempio: IBM, HP, Canon..." %>  

  <%= vendor.submit %> - <%= link_to_back root_path %>  
<% end %>
