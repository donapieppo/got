<ul>
<% @organizations.each do |organization| %>
  <li><%= link_to organization, organization_path(organization) %></li>
<% end %>
</ul>

<% if current_user.is_cesia? %>  
  <%= link_to_new 'Nuova struttura', new_organization_path %>
<% end %>

