<dl>
<dt>Toner compatibili:</dt>
<% if @compatible_toners.empty? %>  
  Registrazione incompleta. Si prega di aggiungere i modelli di toner compatibili.
<% end %>
<% @compatible_toners.each do |compatible_toner| %>  
  <dd><%= link_to compatible_toner, compatible_toner %></dd>
<% end %>

<% if @available_toners.empty? %>  
  <dt class="alert alert-warning">Non sono al momento disponibili toner compatibili con questo modello di toner in Unibo.</dt>
<% else %>
  <dt>Sono disponibili toner presso</dt>
  <% @available_toners.each do |available_toner| %>
    <dd><%= available_toner.organization %> (<%= available_toner.organization.admins_string %>)</dd>
  <% end %>
<% end %>

<% if (models = @printer_model.printers).any? %>  
  <dt>Stampanti di questo modello si trovano presso:</dt>
  <% models.map(&:organization).uniq.each do |organization| %>  
    <dd><%= organization %></dd>
  <% end %>
<% end %>
</dl>
